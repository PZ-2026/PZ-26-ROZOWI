# ============================================================
# Profil: dev
# ============================================================
# Flyway: lokalizacja migracji + osobny katalog z danymi testowymi
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

# Baza danych dev (dostosuj do swojego środowiska lokalnego)
spring.datasource.url=jdbc:postgresql://localhost:5432/blokur_dev
spring.datasource.username=blokur
spring.datasource.password=blokur
