# ============================================================
# Profil: test
# ============================================================
# Flyway: lokalizacja migracji + osobny katalog z danymi testowymi
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

# Baza danych test (np. H2 in-memory lub dedykowana instancja PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/blokur_test
# spring.datasource.username=blokur_test
# spring.datasource.password=blokur_test
